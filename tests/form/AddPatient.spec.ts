// import { expect, mergeTests } from '@playwright/test';
// import { formPage } from '../../fixtures/chatBotTest';
// import { projectConfig } from '../../project.config';

// export const test = mergeTests(formPage);

// test('it should be possible to consult the patients current state of health', async ({
// 	formPage,
// }) => {
// 	const { form, utilityPages } = formPage;

// 	await utilityPages.goto(projectConfig.baseURL);
// 	await expect(form.titlePage).toBeVisible();

// 	await form.fillForm();
// 	await form.submitForm();
// });
